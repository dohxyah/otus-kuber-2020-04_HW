apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ name }}-pv
spec:
  persistentVolumeReclaimPolicy: Retain
  storageClassName: {{ name }}
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: {{ storage_size }}
  hostPath:
    path: /data/{{ name }}-pv/