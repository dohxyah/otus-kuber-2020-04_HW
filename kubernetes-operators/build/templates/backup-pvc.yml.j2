apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "backup-{{ name }}-pvc"
spec:
  storageClassName: backup-{{ name }}
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"

