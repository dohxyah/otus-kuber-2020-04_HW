apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ name }}-pvc"
spec:
  storageClassName: {{ name }}
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: {{ storage_size }}
